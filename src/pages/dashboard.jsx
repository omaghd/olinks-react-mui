import Box from "@mui/material/Box";
import LinkForm from "../components/LinkForm";

const Dashboard = () => {
  return (
    <Box flex={10} p={1}>
      <LinkForm />
    </Box>
  );
};

export default Dashboard;
